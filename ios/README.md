# Flutter iOS Configuration

This directory contains iOS-specific files.

To generate complete iOS project files, run:
```bash
flutter create .
```

This will create all necessary iOS project files and configurations.
